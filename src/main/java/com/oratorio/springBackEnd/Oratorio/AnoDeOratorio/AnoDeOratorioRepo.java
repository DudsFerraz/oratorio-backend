package com.oratorio.springBackEnd.Oratorio.AnoDeOratorio;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AnoDeOratorioRepo extends JpaRepository<AnoDeOratorio, Long> {
}
